# Getting Started with TinyEye

```bash
python -m pip install -e ".[dev]"
make fixtures && make examples
ls examples/out/*eye.jpg examples/out/*eye.md
```

Expect `mug.eye.md` to honor `--belief` and swatch sidecars to keep Belief empty.

See [HOW_TO_VERIFY.md](../../HOW_TO_VERIFY.md).
