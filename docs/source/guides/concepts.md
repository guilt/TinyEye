# Core Concepts

Watch does not see. TinyEye imports files a human (or a packer) already has.

Width, height, mean RGB may appear in the sidecar. They describe the raster.
They do not name the object.

JPEG + md is the memory. A TAESD latent is a compressed extra for desktop
experiments. NanoToT child drops raw media either way.
