[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# JPEG + sidecar path is stdlib + optional Pillow. Latent extra pulls torch.

[dev-packages]
pytest = ">=7.0"
pytest-cov = "*"
coverage = "*"
ruff = ">=0.5.6"
build = "*"
Pillow = "*"

[requires]
python_version = "3.9"
